{
    "name": "avramovic/gumpress",
    "description": "Drop-in Gumroad licensing for WordPress plugins and themes.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.0"
    },
    "suggest": {
        "ext-openssl": "Encrypts the cached licence state at rest in wp_options. Without it GumPress still works, but that cache (which holds the buyer's email and checkout fields) is stored in plaintext."
    },
    "require-dev": {
        "php-stubs/wordpress-stubs": "^6.6",
        "phpstan/phpstan": "^2.0",
        "phpunit/phpunit": "^9.6 || ^10.5",
        "szepeviktor/phpstan-wordpress": "^2.0"
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit",
        "lint": "php bin/lint.php gumpress bin",
        "stan": "phpstan analyse",
        "build": "php bin/build.php",
        "encrypt": "php bin/encrypt.php"
    }
}
